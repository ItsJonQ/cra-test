{
  "name": "cra-test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-scripts": "3.1.1"
  },
  "devDependencies": {
    "glob": "7.1.4",
    "react-app-rewire-disable-chunks": "0.0.1",
    "react-app-rewire-micro-frontends": "0.0.1",
    "react-app-rewired": "2.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build:app": "react-app-rewired build",
    "build:loader": "node scripts/generate-loader-script.js",
    "build": "npm run build:app && npm run build:loader",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "config-overrides-path": "node_modules/react-app-rewire-disable-chunks",
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
